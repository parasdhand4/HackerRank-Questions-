struct node{          // declaring a linked list 
    int data;
    struct node *next;  
 };
 
 typedef struct node *node1; 
 
 
OR


struct LinkedList{          // declaring a linked list 
    int data;
    struct LinkedList *next;  
 };
 
 typedef struct LinkedList *node; 
 
 
